# Database Configuration
DATABASE_URL=sqlite:///./ops_mesh.db

# Google Cloud Configuration (Optional - fallback implementations are used)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json
GOOGLE_CLOUD_REGION=us-central1

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Ops Mesh Backend

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://127.0.0.1:3000","http://127.0.0.1:3001"]

# Redis Configuration (for future use)
REDIS_URL=redis://localhost:6379

# WebSocket Configuration
WEBSOCKET_PATH=/ws

# Ops Mesh Configuration
OPS_MESH_TOPIC_PREFIX=ops-mesh
OPS_MESH_MONITORING=true
OPS_MESH_LOGGING=true

# Development Settings
DEBUG=false
RELOAD=false